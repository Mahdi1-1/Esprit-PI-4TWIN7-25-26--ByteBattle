# Checklist: Task 1.3 — `CommandPalette` component

## File: `frontend/src/components/CommandPalette.tsx`

### Rendering
- [ ] Rendered via `ReactDOM.createPortal(jsx, document.body)`
- [ ] `z-[9999]` z-index
- [ ] Semi-transparent backdrop overlay
- [ ] Centered modal, `max-w-lg`
- [ ] Styled with design system vars: `var(--surface-1)`, `var(--border-default)`, `var(--brand-primary)`

### Search
- [ ] Search `<input>` auto-focused on open
- [ ] Filtering: `label.toLowerCase().includes(query.toLowerCase())`
- [ ] Empty query: shows recent commands (from localStorage `bb2_recent_commands`) + all commands
- [ ] Non-empty query: shows only matching commands
- [ ] Commands grouped by category with section headers

### Command display
- [ ] Each row shows: icon, label, category tag, shortcut badge (if exists)
- [ ] Guest users see only public commands
- [ ] Auth-gated commands hidden when not logged in

### Keyboard navigation
- [ ] `↑` / `↓`: move `selectedIndex` (wrap around at boundaries)
- [ ] `Enter`: execute `filteredCommands[selectedIndex].action()`, close palette, save to recent
- [ ] `Escape`: close palette
- [ ] `Ctrl+K` again: close palette (toggle)

### Accessibility
- [ ] `role="dialog"`, `aria-modal="true"`, `aria-label="Command Palette"`
- [ ] Command items: `role="option"` inside `role="listbox"`
- [ ] `aria-activedescendant` tracks current selection
- [ ] Focus returns to previous element on close

### Behavior
- [ ] Recent commands saved to `localStorage` (`bb2_recent_commands`, max 5)
- [ ] `prefers-reduced-motion: reduce` → skip entrance animation
- [ ] Clicking backdrop closes palette
- [ ] TypeScript compiles with no errors
