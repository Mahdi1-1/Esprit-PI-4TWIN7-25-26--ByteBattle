# Checklist: Task 1.1 — `useKeyboardShortcuts` hook

## File: `frontend/src/hooks/useKeyboardShortcuts.ts`

- [ ] `KeyboardShortcut` interface exported with all fields: `id`, `key`, `ctrl?`, `shift?`, `alt?`, `action`, `description`, `category`, `when?`, `ignoreInputFocus?`
- [ ] `ShortcutCategory` type exported
- [ ] `useKeyboardShortcuts(shortcuts: KeyboardShortcut[]): void` exported
- [ ] Hook calls `registerShortcuts()` from context on mount
- [ ] Hook calls `unregisterShortcuts()` from context on unmount
- [ ] Hook uses `useEffect` with `shortcuts` as dependency (properly memoized)
- [ ] No direct `window.addEventListener` in this hook
- [ ] TypeScript compiles with no errors
