# Checklist: Task 1.2 — `KeyboardShortcutsContext`

## File: `frontend/src/context/KeyboardShortcutsContext.tsx`

- [ ] `KeyboardShortcutsProvider` component exported
- [ ] `useKeyboardShortcutsContext()` hook exported
- [ ] Single `window.addEventListener('keydown', handler)` in `useEffect`
- [ ] Listener cleaned up on unmount
- [ ] Key normalization: `[ctrl+][shift+][alt+]<key.toLowerCase()>`
- [ ] Map lookup is O(1) via `Map<string, RegisteredShortcut>`
- [ ] `when()` guard checked before action fires
- [ ] Input-focus suppression: single-key shortcuts (no modifier) suppressed when focus is in `input`, `textarea`, `select`, `[contenteditable]`, `.monaco-editor`
- [ ] Modifier shortcuts (`ctrl`, `alt`, `shift` present) always fire regardless of focus
- [ ] `e.preventDefault()` called when a shortcut fires
- [ ] `registerShortcuts(shortcuts[])` adds to map
- [ ] `unregisterShortcuts(ids[])` removes from map
- [ ] `getAllShortcuts()` returns full registry (for help modal)
- [ ] `isCommandPaletteOpen` / `setCommandPaletteOpen` state exposed
- [ ] `isHelpModalOpen` / `setHelpModalOpen` state exposed
- [ ] No memory leaks: shortcuts properly cleaned up
- [ ] TypeScript compiles with no errors
