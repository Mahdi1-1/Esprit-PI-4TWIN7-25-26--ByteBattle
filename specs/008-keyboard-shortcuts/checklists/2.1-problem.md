# Checklist: Task 2.1 — Problem.tsx Editor Shortcuts

## File: `frontend/src/pages/Problem.tsx`

- [ ] `useKeyboardShortcuts` imported and called
- [ ] `Ctrl+Enter` → `handleRun()` registered (with `when: !isMonacoFocused`)
- [ ] `Ctrl+Shift+Enter` → `handleSubmit()` registered (with `when: !isMonacoFocused`)
- [ ] `F11` → toggle fullscreen registered
- [ ] Monaco `editor.addAction()` for `Ctrl+Enter` → `handleRun()` (fires from inside editor)
- [ ] Monaco `editor.addAction()` for `Ctrl+Shift+Enter` → `handleSubmit()` (fires from inside editor)
- [ ] `isMonacoFocused()` helper: `document.activeElement?.closest('.monaco-editor') !== null`
- [ ] No conflict with anticheat (`useAnticheat` continues to work)
- [ ] Existing copy/paste blocking in Monaco `onKeyDown` unchanged
- [ ] TypeScript compiles with no errors
