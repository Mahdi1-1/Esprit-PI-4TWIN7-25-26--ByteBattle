# Checklist: Task 2.2 — DuelRoom.tsx Editor Shortcuts

## File: `frontend/src/pages/DuelRoom.tsx`

- [ ] `useKeyboardShortcuts` imported and called
- [ ] `Ctrl+Enter` → `handleTestCode()` registered (with `when: !isMonacoFocused`)
- [ ] Monaco `editor.addAction()` for `Ctrl+Enter` → `handleTestCode()` (fires from inside editor)
- [ ] No conflict with anticheat (`useAnticheat` continues to work)
- [ ] TypeScript compiles with no errors
